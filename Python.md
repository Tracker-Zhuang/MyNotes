# Python的伪私有化机制
Python中不存在只能从对象内部访问的“私有(private)”变量，但Python制定了以下公约：

1. 以单下划线开头的成员变量或方法，是设计者不想暴露给外部的API，使用者不应该进行访问。

2. 以双下划线开头且以至多一个下划线结尾的成员变量或方法，会进行名称转写。

以双下划线开头，并以最多一个下划线结尾的标识符，例如__X会被转写为_classname__X，其中classname为类名。

![示例1](images\Python_名称转写.png)

以两个下划线结尾时不行
![示例2](images\Python_名称转写2.png)

在上面的示例中，我们可以通过_MyClass__private_var的方式访问到私有变量__private_var的值。尽管这样做是合法的，但并不推荐在外部代码中直接访问私有变量。这是因为私有变量的名称修饰是由Python解释器自动完成的，而且可能因为Python版本或其他因素而有所不同。
![示例3](images\Python_名称转写3.png)